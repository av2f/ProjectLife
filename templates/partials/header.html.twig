<nav id="navhome" class="navbar fixed-top navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{ path('btj_home') }}">
        <img src="/img/Librairy/maptrip.png" width="30" height="30" class="d-inline-block align-top" alt="">
        &nbsp;BizTripJoy
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor03">
        {% if app.user %}
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('btj_main') }}"><i class="fas fa-home"></i>&nbsp;Accueil<span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#"><i class="fas fa-envelope"></i>&nbsp;Messages</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#"><i class="fas fa-plane-departure"></i>&nbsp;Mes BizTrip</a>
                </li>
            </ul>
            <ul class="navbar-nav ml-auto mr-2">
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
                        <img src="{{ app.user.avatar }}" class="avatar avatar-mini" alt="Avatar de {{ app.user.pseudo }}">
                        &nbsp;{{ app.user.pseudo }}
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                        <a href="#" class="dropdown-item">Créer un BizTrip</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('btj_profile', {'id':app.user.id}) }}" class="dropdown-item">Mon profil</a>
                        <a href="#" class="dropdown-item">Mon abonnement</a>
                        <a href="#" class="dropdown-item">Modifier mon mot de passe</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('btj_logout') }}" class="dropdown-item">Deconnexion</a>
                    </div>
                </li>
            </ul>
        {% else %}
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="#">Concept<span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#price">Abonnement</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#price">Contact</a>
                </li>
            </ul>
            <a href="{{ path('btj_login') }}">
                <button type="button" class="btn btn-primary mr-2">Me connecter</button>
            </a>
        {% endif %}
    </div>
</nav>